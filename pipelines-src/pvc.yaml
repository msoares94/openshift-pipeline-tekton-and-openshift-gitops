kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: workspace-teste1
  namespace: spring-pipeline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2
  volumeMode: Filesystem

